{
  "name": "header-parsers",
  "version": "0.0.1",
  "keywords": [
    "http",
    "header",
    "range",
    "parser",
    "semantics"
  ],
  "files": [
    "./dist"
  ],
  "scripts": {
    "test": "node --require ts-node/register --test './src/**/*.test.ts'",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
